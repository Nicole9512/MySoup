MySoup
====================

* 记录一些笔记、心得、开发中遇见的小坑。


## 一、目录
```
├── django                            // python-django框架
├── grab                              // python爬虫脚本
├── note                              // 笔记
│   ├── database                      // 数据库相关(SQL and noSQL)
│   ├── dataStructures                // 数据结构
│   ├── designPatterns                // 设计模式
│   ├── font-end                      // 前端
│   ├── Internet                      // 计算机网络
│   ├── interview                     // 面试
│   ├── nginx                         // nginx
│   │   └── config                    // 模板配置（不完整）
│   │       └── vhost.d               // nginx-server配置（php-restful）
│   └── OS                            // 操作系统
├── script                            // 很久前的excel相关脚本，写的垃圾，有时间重写
├── soup                              // 开发中遇见的坑、心得、总结
└── yii2                              // php-yii2框架
```

## 二、说明
* 这里会持续更新，如果你觉得有用，不要吝惜你的Star。
